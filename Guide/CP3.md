## 实验目的

- 认识到渲染是可以分多阶段执行的（即一组着色器代码可以代表一个阶段）
- 了解FrameBuffer的作用
- 认识到渲染目标可以不仅仅是屏幕
- 认识到如何多阶段渲染过程中，存储渲染结果用的纹理

## 实验任务

1. 运行程序（需要结果截图）
2. 简要说明渲染流程
3. 增加一个渲染步骤，将现阶段的第二步的渲染结果也保存到FramBuffer内，然后第三步的渲染操作中添加一个黄色矩形到画面中，使得最终画面呈现为白色背景下，一个模糊的三角形和一个清晰的黄色矩形（需要结果截图）