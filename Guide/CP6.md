## 实验目的

- 了解obj模型的文件数据格式
- 学会如何加载外部三维模型
- 体会基于PBR渲染与Phong光照模型渲染效果的不同
- 学会如何使用AI生成简单的3D模型

## 实验任务

1. 运行程序（需要结果截图）
2. 简要回答obj文件的包含哪些内容信息，以及是如何标识的
3. 找到模型读取位置，尝试从batmanCar切换到car模型（需要结果截图）
4. 阅读objLoader，再导入外部模型时除了加载基本信息还进行了哪些操作
5. 前往https://3d.hunyuan.tencent.com/利用图生3D生成一个包含几何+纹理的3D模型文件（模型面数设置为50k，否则加载时间可能会过长），然后下载时切换为obj模型，然后放到对应位置，导入运行。（需要结果截图）